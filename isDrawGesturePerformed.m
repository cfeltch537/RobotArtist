function [ draw ] = isDrawGesturePerformed( input_args )
%ISDRAWGESTUREPERFORMED This should make use of the My EMG Data and return
%true if the drawing gesture is being performed.

    if something == true
        draw = true;
    else
        draw = false
    end
    
end

